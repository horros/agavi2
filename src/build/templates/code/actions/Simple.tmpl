

	/**
	 * Whether or not this controller is "simple", i.e. doesn't use validation etc.
	 *
	 * @return     bool true, if this controller should act in simple mode, or false.
	 *
	 */
	public function isSimple()
	{
		return true;
	}