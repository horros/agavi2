

	/**
	 * Handles the %%OUTPUT_TYPE_NAME%% output type.
	 *
	 * @parameter  RequestDataHolder the (validated) request data
	 *
	 * @return     mixed <ul>
	 *                     <li>An ExecutionContainer to forward the execution to or</li>
	 *                     <li>Any other type will be set as the response content.</li>
	 *                   </ul>
	 */
	public function execute%%OUTPUT_TYPE_NAME%%(RequestDataHolder $rd)
	{
		$this->setup%%OUTPUT_TYPE_NAME%%($rd);

		$this->setAttribute('_title', '%%CONTROLLER_NAME%%');
	}